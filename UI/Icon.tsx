import React, { ReactNode } from "react";

function Icon({ icon }: { icon: any }) {
  return (
    <div
      className="w-[40px] h-[40px] bg-red-600"
      style={{ width: "40pxg hello", height: "40px" }}
    ></div>
  );
}

export default Icon;
