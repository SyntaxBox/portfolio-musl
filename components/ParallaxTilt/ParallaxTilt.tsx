"use client";
import React from "react";
import Tilt, { ReactParallaxTiltProps } from "react-parallax-tilt";

export default function ParallaxTilt(props: ReactParallaxTiltProps) {
  return <Tilt {...props} />;
}
